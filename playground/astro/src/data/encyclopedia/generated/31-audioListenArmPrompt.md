---
code: 31
name: audioListenArmPrompt
group: "checkAudio (voice plane gates; meta #97 Track E / epic #92) TCB only — no waveforms / PCM. sessiond checks before voiceArm / inject."
generated: true
---

# audioListenArmPrompt (31)

Schema: `schema/policy.capnp` enum `PolicyReason`.

**Group:** checkAudio (voice plane gates; meta #97 Track E / epic #92) TCB only — no waveforms / PCM. sessiond checks before voiceArm / inject.

Confirm class for listen arm; hard seats fail closed on prompt.

Stub generated by `playground/scripts/gen-encyclopedia.py`.
Product notes (when present) live in `playground/astro/src/lib/encyclopedia-notes.ts`.
